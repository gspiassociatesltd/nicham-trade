'use client'
import { useState } from 'react'
export default function Agent(){const [code,setCode]=useState('');const [name,setName]=useState('');const [phone,setPhone]=useState('');const gen=()=>{if(!name||!phone)return alert('Enter name phone');const c='NICHAM-BUY-'+name.slice(0,3).toUpperCase()+'-'+phone.slice(-4);setCode(c);localStorage.setItem('nicham_buyer_code',c)};const share=()=>{const link=`${window.location.origin}/?ref=${code}`;window.open(`https://wa.me/?text=${encodeURIComponent('NiChAm-Trade Chemicals + Solar: '+link+' Perpetual commission')}`,'_blank')};return (<main className="min-h-screen bg-[#f6f7e8] p-4"><a href="/" className="text-sm font-bold">← Back</a><div className="max-w-xl mx-auto bg-white rounded-2xl shadow p-6 mt-4"><h1 className="font-black">Buyer Agent - Perpetual Commission</h1><div className="mt-4 grid grid-cols-2 gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Name" className="px-3 py-2 rounded-full border text-xs"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone" className="px-3 py-2 rounded-full border text-xs"/></div><button onClick={gen} className="mt-3 w-full py-2 bg-black text-white rounded-full font-bold text-sm">Generate Code</button>{code && <div className="mt-4 bg-green-50 p-3 rounded-xl"><div className="font-black text-sm">{code} - Perpetual</div><button onClick={share} className="mt-2 w-full py-2 bg-green-600 text-white rounded-full font-bold text-xs">Share WhatsApp with Perpetual Link</button></div>}</div></main>) }
