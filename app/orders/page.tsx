"use client"
import { useEffect, useState } from "react"
import Link from "next/link"
export default function Orders(){const [orders,setOrders]=useState<any[]>([]);useEffect(()=>{const s=localStorage.getItem("nicham_orders");if(s)try{setOrders(JSON.parse(s))}catch{}},[]);return <div className="min-h-screen bg-[#FFFEF5]"><header className="bg-white border-b"><div className="max-w-4xl mx-auto px-4 py-3"><Link href="/" className="text-xs border px-4 py-2 rounded-full">← Marketplace</Link></div></header><div className="max-w-4xl mx-auto p-4"><h1 className="font-black text-xl">Orders ({orders.length})</h1>{orders.map((o:any)=><div key={o.id} className="bg-white rounded-xl p-3 border mt-2"><div className="font-bold text-sm">{o.product}</div><div className="text-xs">N{o.total.toLocaleString()} • {o.status}</div></div>)}</div></div>}
