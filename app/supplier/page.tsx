'use client'
import { useState } from 'react'
export default function Supplier(){const [code,setCode]=useState('');const [name,setName]=useState('');const [wechat,setWechat]=useState('');const gen=()=>{if(!name||!wechat)return alert('Enter name WeChat');const c='NICHAM-SUP-'+name.slice(0,3).toUpperCase()+'-'+Date.now().toString().slice(-4);setCode(c);localStorage.setItem('nicham_supplier_code',c)};return (<main className="min-h-screen bg-[#f6f7e8] p-4"><a href="/" className="text-sm font-bold">← Back</a><div className="max-w-xl mx-auto bg-white rounded-2xl shadow p-6 mt-4"><h1 className="font-black">China Supplier Agent - Perpetual</h1><div className="mt-4 grid grid-cols-2 gap-2"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Name e.g. Li Wei" className="px-3 py-2 rounded-full border text-xs"/><input value={wechat} onChange={e=>setWechat(e.target.value)} placeholder="WeChat ID" className="px-3 py-2 rounded-full border text-xs"/></div><button onClick={gen} className="mt-3 w-full py-2 bg-green-600 text-white rounded-full font-bold text-sm">Generate Supplier Code - Earn 2% Perpetually</button>{code && <div className="mt-4 bg-green-50 p-3 rounded-xl font-black">{code}</div>}</div></main>) }
